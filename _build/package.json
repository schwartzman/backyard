{
  "name": "backyard-build",
  "description": "backyard cafe build module",
  "private": "true",
  "license": "WTFPL",
  "repository": {},
  "dependencies": {
    "node-sass": "latest",
    "autoprefixer": "latest",
    "postcss-cli": "latest"
  },
  "scripts": {
    "sass": "node-sass --output-style expanded --output ../library/css/ ../library/scss/",
    "postsass": "postcss --use autoprefixer --replace ../library/css/*"
  }
}
